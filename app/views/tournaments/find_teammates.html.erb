<table class="current-team-table responsive">
   <thead>
     <tr>
       <th>Current Team Table</th>
       <% current_user.player.bracket_pool_players.each do |x| %>
        <th>
          Team Name: <%= x.team.team_name %>
        </th>
       <% end %>
     </tr>
     <tr>
       <th>Player nickname: </th>
       <th>Player points: </th>
       <th>Invite</th>
     </tr>
   </thead>
   <tbody>
     <% current_user.player.bracket_pool_players.each do |x| %>
       <% x.team.bracket_pool_players.each do |y| %>
     <tr>
       <td>
         <div class="current-team-item-category">  <em> <%= y.player.nickname %></em> </div>
       </td>
       <td>
         <div class="current-team-item-category">  <em> <%= y.player.points %> </em> </div>
       </td>
       <td>
        <div class="current-team-item-category">
          <%= button_to "kick", { remote: true}, {class: 'invite-teammates-btn'} %>
        </div>
       </td>
     </tr>
     <% end %>
     <% end %>
   </tbody>
</table>

<table class="join-teammates-table responsive">
   <thead>
     <tr>
       <th>Join Teammates Table</th>
     </tr>
     <tr>
       <th>Bracket Pool Player ID: </th>
       <th>Player nickname: </th>
       <th>Player points: </th>
       <th>Invite</th>
     </tr>
   </thead>
   <tbody>
     <% current_user.player.bracket_pools.all.each do |x| %>
     <% x.bracket_pool_players.each do |y| %>
     <tr>
       <td>
         <div class="join-teammates-item-category">  <em> <%= y.id %></em> </div>
       </td>
       <td>
         <div class="join-teammates-item-category">  <em> <%= y.player.user.nickname %> </em> </div>
       </td>
       <td>
         <div class="join-teammates-item-category">  <em> <%= y.player.points %> </em> </div>
       </td>
       <td>
        <div class="join-teammates-item-category">
          <%= button_to "Join", { remote: true}, {class: 'join-teammates-btn'} %>
        </div>
       </td>
     </tr>
     <% end %>
     <% end %>
   </tbody>
</table>

<table class="find-teammates-table responsive">
   <thead>
     <tr>
       <th>Find Teammates Table</th>
     </tr>
     <tr>
       <th>Bracket Pool Player ID: </th>
       <th>Player nickname: </th>
       <th>Player points: </th>
       <th>Invite</th>
     </tr>
   </thead>
   <tbody>
     <% current_user.player.bracket_pools.all.each do |x| %>
     <% x.bracket_pool_players.each do |y| %>
     <tr>
       <td>
         <div class="find-teammates-item-category">  <em> <%= y.id %></em> </div>
       </td>
       <td>
         <div class="find-teammates-item-category">  <em> <%= y.player.user.nickname %> </em> </div>
       </td>
       <td>
         <div class="find-teammates-item-category">  <em> <%= y.player.points %> </em> </div>
       </td>
       <td>
        <div class="find-teammates-item-category">
          <%= button_to "Invite", { remote: true}, {class: 'invite-teammates-btn'} %>
        </div>
       </td>
     </tr>
     <% end %>
     <% end %>
   </tbody>
</table>
